<?xml version="1.0" encoding="UTF-8"?>
<!-- This document was created with Syntext Serna Free. -->
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
"docbookV4.5/docbookx.dtd">
<refentry>
  <refnamediv>
    <refname>list</refname>

    <refpurpose>List contents of SciDB namespace</refpurpose>
  </refnamediv>

  <refsynopsisdiv>
    <synopsis>list(<replaceable>element</replaceable>[,<replaceable>version_flag</replaceable>])</synopsis>
  </refsynopsisdiv>

  <refsect1>
    <title>Summary</title>

    <para><indexterm>
        <primary>list arrays</primary>
      </indexterm><indexterm>
        <primary>arrays</primary>

        <secondary>versions</secondary>
      </indexterm><indexterm>
        <primary>versions</primary>

        <secondary>arrays</secondary>
      </indexterm>The list operator allows you to get a list of elements in
    the current SciDB instance. The input is one of the following
    strings:<informaltable frame="all">
        <tgroup cols="2">
          <colspec colname="c1" colwidth="20*"/>

          <colspec colname="c2" colwidth="60*"/>

          <tbody>
            <row>
              <entry>aggregates</entry>

              <entry>Show all operators that take as input a SciDB array and
              return a scalar.</entry>
            </row>

            <row>
              <entry>arrays</entry>

              <entry>Show all arrays. Each function will be listed with its
              available datatypes and the library in functions which it
              resides. If <replaceable>version_flag</replaceable> is true, the
              operator lists all versions of each array. Thus, if an array has
              n versions, the output will include n+1 rows for that
              array.</entry>
            </row>

            <row>
              <entry>functions</entry>

              <entry>Show all functions and the libraries in which they
              reside.</entry>
            </row>

            <row>
              <entry>instances</entry>

              <entry>Show all SciDB instances. Each instance will be listed
              with its port, id number, and time and date stamps for when it
              came online.</entry>
            </row>

            <row>
              <entry>libraries</entry>

              <entry>Show all libraries that are loaded in the current SciDB
              session.</entry>
            </row>

            <row>
              <entry>operators</entry>

              <entry>Show all operators and the libraries in which they
              reside.</entry>
            </row>

            <row>
              <entry>types</entry>

              <entry>Show all the datatypes the SciDB supports.</entry>
            </row>

            <row>
              <entry>queries</entry>

              <entry>Show all active queries. Each active query will have an
              id, a time and date when it was queries initiated, an error
              code, whether it generated any errors, and a status (boolean
              flag where TRUE means that the query is idle).</entry>
            </row>
          </tbody>
        </tgroup>
      </informaltable></para>
  </refsect1>

  <refsect1>
    <title>Example</title>

    <informalexample>
      <programlisting language="test">--afl store(build(&lt;val:double&gt;[i=0:4,5,0],0),vector0); --show-query=no --show-output=no
--afl store(build(&lt;val:string&gt;[i=0:4,5,0],''),vector1); --show-query=no --show-output=no</programlisting>

      <para>This example shows sample output from the <literal>list</literal>
      operator.</para>

      <procedure>
        <step>
          <para>List all arrays:<programlisting language="test">--afl list('arrays'); --show-output-in-test-file=no</programlisting></para>
        </step>

        <step>
          <para>List all versions of all arrays:<programlisting
          language="test">--afl list('arrays',true); --show-output-in-test-file=no</programlisting></para>
        </step>

        <step>
          <para>List all SciDB aggregates:<programlisting language="test">--afl list('aggregates');</programlisting></para>
        </step>
      </procedure>
    </informalexample>
  </refsect1>
</refentry>
