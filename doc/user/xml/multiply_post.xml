<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "docbookV4.5/docbookx.dtd">
<refentry>
  <refnamediv>
    <refname>multiply</refname>

    <refpurpose><indexterm>
        <primary>multiply</primary>
      </indexterm><indexterm>
        <primary>matrices</primary>

        <secondary>multiplication</secondary>
      </indexterm>Produces a result array via matrix multiplication of two
    input arrays</refpurpose>
  </refnamediv>

  <refsynopsisdiv>
    <synopsis>AFL%  multiply(<replaceable>left_array</replaceable>,<replaceable>right_array</replaceable>);</synopsis>
  </refsynopsisdiv>

  <refsect1>
    <title>Summary</title>

    <para>The multiply operator performs matrix multiplication on two input
    matrices and returns a result matrix.</para>

    <para>Both inputs must be compatible for the multiply operation, and both
    must have a single numeric attribute.</para>

    <para>If <replaceable>m</replaceable>, <replaceable>n</replaceable>, and
    <replaceable>p</replaceable> represent integers, to be compatible for
    multiplication, two matrices must have dimensions as follows: if
    <replaceable>left_array</replaceable> has dimensions
    <replaceable>m</replaceable><symbol role="symbolfont">×</symbol><replaceable>n</replaceable>, then
    <replaceable>right_array</replaceable> must have dimensions
    <replaceable>n</replaceable><symbol role="symbolfont">×</symbol><replaceable>p</replaceable>, and the result
    array will have dimensions <replaceable>m</replaceable><symbol role="symbolfont">×</symbol><replaceable>p</replaceable>.</para>
  </refsect1>

  <refsect1>
    <title>Example</title>

    <informalexample>
      <para>This example multiplies a 3<symbol role="symbolfont">×</symbol>2
      array and a 2<symbol role="symbolfont">×</symbol>3 array.</para>

      <procedure>
        <step>
          <para>Create a 3<symbol role="symbolfont">×</symbol>2
          array:<para><programlisting>AFL% store(build(&lt;val:double&gt;[i=0:2,3,0,j=0:1,2,0],(i+1)*3+j),left);  </programlisting><screen>[
[(3),(4)],
[(6),(7)],
[(9),(10)]
]</screen></para></para>
        </step>

        <step>
          <para>Create a 2<symbol role="symbolfont">×</symbol>3
          array:<para><programlisting>AFL% store(build(&lt;val:double&gt;[i=0:1,2,0,j=0:2,3,0],(i+1)*3-j),right);  </programlisting><screen>[
[(3),(2),(1)],
[(6),(5),(4)]
]</screen></para></para>
        </step>

        <step>
          <para>Multiply left and right.<para><programlisting>AFL% multiply(left, right)  </programlisting><screen>[
[(33),(26),(19)],
[(60),(47),(34)],
[(87),(68),(49)]
]</screen></para></para>
        </step>
      </procedure>
    </informalexample>
  </refsect1>
</refentry>
