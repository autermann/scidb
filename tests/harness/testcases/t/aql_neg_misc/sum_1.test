# http://trac.scidb.org/ticket/325#comment:18

--setup
--aql create immutable array A <A:int64>[x=1:5,5,0]
store(build(A,x+1),A)

--test
#--error --code=36 --aql="select sum(*) from A group by x as part"
--error --code=scidb::SCIDB_SE_SYNTAX::SCIDB_LE_ATTRIBUTE_NOT_EXIST --aql="select sum(x) from A group by x as part"

--cleanup
--aql drop array A
