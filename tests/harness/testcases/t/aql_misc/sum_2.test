# http://trac.scidb.org/ticket/325#comment:18

--setup
--aql create immutable array A <a:int32>[x=0:2,3,0]
store(build(A,x+1),A)

--test
--aql select sum(a) from A group by x as part
--aql select sum(part) from A group by x as part
--aql select sum(select a from part) from A group by x as part
#--aql select sum(select * from part) from A group by x as part

--cleanup
--aql drop array A
