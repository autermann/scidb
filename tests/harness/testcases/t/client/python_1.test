--setup
--start-query-logging

--test

echo ('python basic.py')
--shell --command "cd ${TESTCASES_DIR}/t/client && python basic.py ${IQUERY_HOST} ${IQUERY_PORT}" --store

--cleanup
--stop-query-logging
