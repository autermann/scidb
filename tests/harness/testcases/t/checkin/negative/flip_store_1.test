# for ticket no. 657

--setup
create array A <a:int64> [x=0:2,3,0]
create empty array C <x:int64> [a=0:3,3,0]
store(build(A,x+10),A)

--test
list()
--error --code=scidb::SCIDB_SE_STORAGE::SCIDB_LE_CHUNK_OUT_OF_BOUNDARIES "redimension_store(A,C)"
scan(C)


--cleanup
remove(A)
remove(C)
list()
